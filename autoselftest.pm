package VCS::Site::autoselftest;
use strict;

use VCS::Vars;

use Data::Dumper;

sub selftest 
# //////////////////////////////////////////////////
{
	my $self = shift;
	
	my $vars = $self->{ 'VCS::Vars' };
	
	# reconnect to fake db
	
	# create test app
	
	# test all subs
	
	# cleaning
	
	# reconnect to normal db
	
	# show all result
}

1;
